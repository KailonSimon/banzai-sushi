import React from 'react'
import cardStyles from '../styles/Hero.module.css'

const MenuItem = (props) => {
    return (
        <div className={cardStyles.card}>
            <h2 className={cardStyles.card}></h2>
            <p className={cardStyles.card}></p>
        </div>
    )
}

export default MenuItem
